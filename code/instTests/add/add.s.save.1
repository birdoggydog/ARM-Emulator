text

_start: .global _start
	.global main
	b main

main:

	mov r0,#1 @put the number 1 in reg 0
	mov r1,#2 @ put the number 2 in reg 0
	add r3,r0,r1 @ add r1 and r0, put the result in r3
	add r4,r0,r3
	
	.end
